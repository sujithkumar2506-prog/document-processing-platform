doubts:
28/7/26 -- solved
Big picture: extract the fields in the doc
1) Extraction process begins with:
	1) receiving chart id(fax id)
	2) with that id, download the scanned doc from blob storage
	3) Every region's documents not in same structure, different layouts specific to region.
	4) So, region specific models are used to extract fields, for that need to classify the region of the fax
	5) Once classified, Use that region specific AI model to extract fields, the output of this stage is JSON data containing fields
	6) write back doc+json in blob storage and update in cosmos db.

DOUBTS: 
	1) how to upload a fax, where to upload(site??)
	2) what is present in cosmos db. before and after Extraction process.
	3) All the 8 models, like KPMAS,KPWA the suffix refer to their region?? like WA stands for Washington.GA Georgia,CA for California?
	4) Upon testing the extraction.py where the sample fax are uploaded.
	5) Noticed gvtmp folder contains the output of extraction json+pdf

doubts::
	
1) kubectl get quota - Syed
2) why KPMAS is always in Docintl models. despite other model not used in docint. - Aravinth/Pooja
3) AKS rs management:
	1) step list of resources action file highlights - deployment is pod, current ongoing service using kubetlget svc, 
